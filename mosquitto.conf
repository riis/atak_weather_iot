# Allow anonymous access
allow_anonymous true

# Listen on port 1883
listener 1883

# Don't save state on disk
persistence false

# Allow connections over WebSockets (optional, if needed)
listener 9001
protocol websockets

# Log all events
log_type all
